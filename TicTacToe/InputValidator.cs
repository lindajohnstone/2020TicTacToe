using System.Text.RegularExpressions;

namespace TicTacToe
{
    public class InputValidator : IValidator
    {
        public bool IsValid(int[][] board, int x, int y)
        {
            // logic
            // declare regex pattern
            // if board[x][y] == regex, return true
            // else return false
            //Regex regex = (d,d);
            return false;
        }
    }
}